# import library 
library("RColroBrewer")

# display all color palette by name
display.brewer.all()

# display the Spectral palette
display.brewer.pal(n = 4, name = 'Spectral')

# chech the corresponding hexcode in Spectral palette 
brewer.pal(n = 4, name = "Spectral")